jobber-app.com {
	header {
		# HSTS — force HTTPS for 1 year, include subdomains
		Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"

		# Prevent clickjacking
		X-Frame-Options "SAMEORIGIN"

		# Block MIME-type sniffing
		X-Content-Type-Options "nosniff"

		# CSP — allow self, Google Fonts, Sentry, inline styles (Tailwind), Paddle
		Content-Security-Policy "default-src 'self'; script-src 'self' https://*.paddle.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob:; connect-src 'self' https://*.ingest.sentry.io https://*.paddle.com; frame-src https://*.paddle.com; object-src 'none'; base-uri 'self'"

		# Referrer policy
		Referrer-Policy "strict-origin-when-cross-origin"

		# Permissions policy — disable unused browser features
		Permissions-Policy "camera=(), microphone=(), geolocation=()"

		# Remove server header
		-Server
	}

	reverse_proxy frontend:80
}
